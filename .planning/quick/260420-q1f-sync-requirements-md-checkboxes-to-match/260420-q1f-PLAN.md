---
id: 260420-q1f
title: Doc sync + session type fix (milestone audit tech debt)
mode: quick
status: complete
date: 2026-04-20
---

# Quick Task 260420-q1f

## Problem

The v1.0 milestone audit (`.planning/v1.0-MILESTONE-AUDIT.md`) flagged two non-blocker hygiene items that are cheap to close before `/gsd-complete-milestone`:

1. **Stale REQUIREMENTS.md** — 6 checkboxes (ORG-02, CAMP-01, CAMP-02, QUAL-01, QUAL-02, QUAL-03) marked `[ ]` in the body and `Pending` in the traceability table, despite phase SUMMARYs closing each of them.
2. **QUAL-01 regression** — the admin portal commit `44eedf9` added `req.session.isAdmin` and `req.session.impersonating` usages without extending `SessionData`, producing 3 TS errors in `src/routes/admin.ts`.

## Tasks

### T1 — Declare the admin session fields

**Files:** `artifacts/api-server/src/types/session.d.ts`

Add `isAdmin?: boolean` and `impersonating?: boolean` as optional fields on `SessionData`.

**Verify:** `pnpm --filter @workspace/api-server typecheck` returns zero errors.

**Done when:** API server typecheck is clean.

### T2 — Sync REQUIREMENTS.md

**Files:** `.planning/REQUIREMENTS.md`

Flip body checkboxes `[ ]` → `[x]` for ORG-02, CAMP-01, CAMP-02, QUAL-01, QUAL-02, QUAL-03. Update the traceability table rows from `Pending` to `Complete` for the same 6 REQs.

**Verify:** grep `Pending` returns zero rows for these 6 REQs.

**Done when:** All 15 v1 requirements show `[x]` and the traceability table is internally consistent.
